# 给 ChatGPT Sol 的 Night Wardrobe 审计与 UI 优化提示词

你现在负责审计 GitHub 仓库 `https://github.com/25384019/night-wardrobe`。

请先只读检查仓库结构、当前分支、提交历史、README、后端 API、SQLite schema、前端模板/CSS/JS 和现有测试，不要直接修改代码，不要推送提交。结合我在本次对话上传的 UI 截图，完成真实的界面审计：将截图中的每个主要区域、交互、状态和视觉问题映射到具体文件、路由、组件或 CSS 选择器；如果截图与代码不一致，请明确指出证据。

项目约束：保留现有导航、SQLite、ComfyUI 元数据解析、Prompt Version 设计、本机默认监听、路径安全、Folder Ops 和 WAL；不重写全站，不修改 ComfyUI 核心逻辑，不新增外网请求、埋点或遥测。优先小范围、可验证、可回滚的改动。

请输出以下内容：

1. 当前架构和数据流简图，以及已完成能力与仍存在的问题。
2. UI 截图审计：布局层级、信息密度、可发现性、加载/空/错误状态、响应式、可访问性和视觉一致性；每条结论附对应文件和证据。
3. 按 P0/P1/P2 排序的优化方案。每项包含用户影响、根因、精确改动文件/函数、兼容性风险、验收方式；禁止泛泛而谈或建议大规模重写。
4. 重点核对：Prompt current/snapshot/working/history 一致性；版本 create/switch/delete/restore；LoRA 多项展示、ID 关联、来源、权重、手工触发词与自动训练标签；outputs summary/full/workflow 懒加载；角色 inspect；请求序列号防止旧响应覆盖新选择；缺失参数不得伪造默认值。
5. 给出测试矩阵和应运行的 Python 编译、pytest、API/OpenAPI、SQLite schema 检查命令。
6. 最后只输出一个标题为“交给 Codex 的施工提示词”的完整执行提示词。该提示词必须包含：目标、明确文件范围、逐项实现要求、禁止事项、数据库迁移策略、测试命令、验收标准、失败回滚点。要求 Codex 一次性实施并在结束时报告实际修改文件和测试结果。

不要编造不存在的文件、接口或测试；无法确认的内容标记为“需验证”。不要代替 Codex 编写实现代码，也不要自行上传或推送任何内容。
